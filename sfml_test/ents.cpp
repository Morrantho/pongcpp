#include "ents.h"

EntityList ents::getAll() {
	return alive;
}

void ents::tick() {
	//for (Entity e : alive) {
	//	e.tick();
	//}
}

void ents::render(sf::RenderWindow * window) {
	//for (Entity e : alive) {
	//	e.render(window);
	//}
}